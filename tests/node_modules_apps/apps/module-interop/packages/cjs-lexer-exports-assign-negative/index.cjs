var dynamicName = './dep-dynamic.cjs';
var dynamic = require(dynamicName);
Object.keys(dynamic).forEach(function (key) {
  exports[key] = dynamic[key];
});

var dep = require('./dep-static.cjs');
var other = {};
Object.keys(other).forEach(function (key) {
  exports[key] = other[key];
});

Object.keys(dep).forEach(function (key) {
  exports[key] = transform(dep[key]);
});

var _dep = _interopWildcard(require('./dep-static.cjs'));
Object.keys(_dep).forEach(function (key) {
  exports[key] = _dep[key];
});

exports.own = 'own-value';

function transform(value) {
  return value;
}

function _interopWildcard(obj) {
  return obj;
}
