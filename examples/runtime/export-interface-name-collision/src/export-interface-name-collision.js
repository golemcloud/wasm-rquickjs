export const testA100Guest = {
    invoke: (a, b, c) => {
        return `${a}:${b}:${c}`;
    },
};

export const testB100Guest = {
    invoke: (a, b, c, d, e) => {
        return `${a}:${b}:${c}:${d}:${e}`;
    },
};
